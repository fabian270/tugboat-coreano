RewriteEngine On

# Sirve archivos y directorios reales (assets, css, js...)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás va al front controller
RewriteRule ^(.*)$ index.php?ruta=$1 [QSA,L]